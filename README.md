# E‑Commerce — Customer Frontend (Vite + React)

Electra Hub Ecommerce Platform-Front End

## 🧱 Tech Stack

- **React + Vite**
- **MERN Stack**
- **UI library:** react-bootstrap / bootstrap / lucide-react
- **Packages used:** `@google/generative-ai`, `@reduxjs/toolkit`, `@stripe/react-stripe-js`, `@stripe/stripe-js`, `axios`, `bootstrap`, `lucide-react`, `moment`, `react`, `react-bootstrap`, `react-dom`, `react-icons`, `react-paginate`, `react-redux`, `react-router-dom`, `react-toastify`, `sonner`, `stripe`, `@eslint/js`, `@types/react`, `@types/react-dom`, `@vitejs/plugin-react`, `eslint`, `eslint-plugin-react-hooks`, `eslint-plugin-react-refresh`, `globals`, `vite`

## ✨ Dependencies

    "@google/generative-ai": "^0.24.1",
    "@reduxjs/toolkit": "^2.9.0",
    "@stripe/react-stripe-js": "^5.2.0",
    "@stripe/stripe-js": "^8.0.0",
    "axios": "^1.11.0",
    "bootstrap": "^5.3.8",
    "jspdf": "^3.0.3",
    "jspdf-autotable": "^5.0.2",
    "lucide-react": "^0.545.0",
    "moment": "^2.30.1",
    "react": "^19.1.1",
    "react-bootstrap": "^2.10.10",
    "react-dom": "^19.1.1",
    "react-icons": "^5.5.0",
    "react-paginate": "^8.3.0",
    "react-redux": "^9.2.0",
    "react-router-dom": "^7.8.2",
    "react-toastify": "^11.0.5",
    "sonner": "^2.0.7",
    "stripe": "^19.1.0"

## 📂 Project Structure (typical)

```
src/
  components/
  pages/
  hooks/
  store/
  utils/
  App.jsx
  main.jsx
public/
```

## ⚙️ Environment

**.env.example**

```
VITE_APP_API_URL=http://localhost:4001
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_key_here
```

## ▶️ Getting Started

```bash
yarn install
yarn dev     # http://localhost:5173
```
